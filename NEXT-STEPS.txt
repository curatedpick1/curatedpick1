THE CURATED PICK - WHAT TO DO NEXT
==================================

You finished old SETUP.md steps 1-4 using Cloudflare.
Do not repeat them. Follow THIS file from now on.
Your local app already includes offline drafts. No desktop app is needed.

Your website: https://curatedpick1.pages.dev
Your Supabase: https://hrwjogpklvgdvljrhptw.supabase.co
Your repository: https://github.com/curatedpick1/curatedpick1

These addresses come from your current project settings.
If you deliberately change projects/domains later, update them everywhere.

WHAT YOU ARE SETTING UP
- Your PC and your friend's PC each run the same local form.
- Save on this PC = private offline draft, including chosen photos/videos.
- Save to Supabase draft = shared draft; not on the public website.
- Publish product = Supabase -> website page -> Pinterest Pin.
- Publishing needs internet. The remaining automation setup below is required.
- Pinterest public API posting also needs Pinterest's approval.


1. START YOUR LOCAL FORM
-----------------------
Install Node.js 24 once, if it is not installed yet.
Open C:\Users\Stymite\Desktop\Curated\Final in File Explorer.
Double-click Start Studio.cmd.

It installs dependencies on the first launch if needed (internet required once),
starts the local server in the background, and opens your browser.
Next time, just double-click the same file. No commands to type.

Open this address in your browser:
http://127.0.0.1:4333

The server keeps running in the background until it stops or you restart the PC.
Opening the launcher again reuses it. Closing the browser does not stop it.
If you have the OLD terminal-based server open, stop it with Ctrl+C once before
using the new launcher.

The product form opens immediately. No password is needed for offline drafts.
Fill anything you want and click "Save on this PC".
You can close the browser and open that draft later under "On this PC".

Always use the same browser profile and the exact address above.
Drafts are stored in that browser, not in the project folder.
Clearing browser site data/private browsing can remove them.
Choosing a file saves its contents when you click Save on this PC.
Pasting an online image URL does not download it for offline viewing.


2. ENABLE YOUR EDITOR LOGIN ONCE
--------------------------------
You already ran the first two SQL migrations. There is ONE new editor migration.

a) Open https://supabase.com/dashboard and select your existing project.
b) Open SQL Editor -> New query.
c) Scroll to "SQL A" near the bottom of THIS file.
d) Copy only the SQL between BEGIN SQL A and END SQL A into the editor.
e) Click Run. This SQL can be rerun if you already applied this editor migration.
   Do not rerun the old first/second migrations afterward.

Now create your editor account:

f) Open Authentication -> Users -> Add user -> Create new user.
g) Enter your email and set the editor password you chose. Confirm the email.
   This is a new APP account. The password is checked by Supabase, not hardcoded
   in the local app. No display name is needed.
h) Copy the new user's ID (the long UUID).
i) Open Table Editor -> catalog_editors -> Insert row.
j) Paste the UUID into user_id. Leave label blank. Save.

In the local app, click "Unlock publishing".
Open "One-time settings". Enter the editor account email you just created,
then enter:

Supabase project URL:
https://hrwjogpklvgdvljrhptw.supabase.co

Publishable key:
Copy your sb_publishable_... key from Supabase API Keys.

Public website URL:
https://curatedpick1.pages.dev

Click Save settings. It closes the settings section.
Now enter only the editor password and click Unlock.
Future visits ask for the password only; the email is remembered in this browser.
Your local form is preserved. Reloading the page locks publishing again.
Only the publishable key belongs in settings. No secret/service-role key.


3. CHECK ONE PRODUCT BEFORE AUTOMATION
---------------------------------------
a) Add a title, description, category and tags.
b) Choose a website poster and describe the image.
c) Add your matching Amazon/AliExpress/other affiliate links under Where to buy.
d) Turn "Post to Pinterest" OFF for this first check.
e) Open "More actions & publishing status", then click "Save to Supabase draft".
f) Confirm the product appears in Supabase -> Table Editor -> products.

That proves the form, login, uploads and database work together.
It is still a draft, so it will not appear on the website yet.
You no longer need to fill product rows manually in Supabase.


4. TURN ON AUTOMATIC WEBSITE UPDATES - OWNER ONLY
-------------------------------------------------
First get your Cloudflare deploy hook:

a) Open Cloudflare -> Workers & Pages -> curatedpick1 -> Settings.
b) Find Builds & deployments -> Deploy hooks.
c) Create a hook called "Supabase catalog", using branch main.
d) Copy the private hook URL. If you already made one in old step 4, use it.

Open PowerShell for this one-time backend setup. The daily form launcher does
not require these commands again:

cd C:\Users\Stymite\Desktop\Curated\Final
npx supabase login

Follow the CLI login prompt in your browser, then run:

npx supabase link --project-ref hrwjogpklvgdvljrhptw

If prompted, enter the database password you saved when creating Supabase.
Do not run db push; your SQL was already applied through the dashboard.

If .env.functions does not exist, create it with:

Copy-Item .env.functions.example .env.functions

Do not overwrite it if you already filled it in.
Generate a new private publisher secret once:

node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"

Copy the printed value. Use this SAME value wherever PUBLISHER_SECRET is needed.
Open .env.functions in your editor and set:

SITE_URL=https://curatedpick1.pages.dev
SITE_DEPLOY_HOOK=PASTE_YOUR_PRIVATE_CLOUDFLARE_HOOK
PUBLISHER_SECRET=PASTE_THE_GENERATED_SECRET
PINTEREST_APP_ID=YOUR-APP-ID
PINTEREST_APP_SECRET=YOUR-APP-SECRET
PINTEREST_API_ENV=sandbox
PINTEREST_STANDARD_ACCESS=false
PINTEREST_PUBLISHING_ENABLED=false

Leave the Pinterest app placeholders for now. Save the file.
Run:

npx supabase secrets set --env-file .env.functions
npx supabase functions deploy publisher --use-api

These commands upload private settings and deploy the publisher to Supabase.
Docker is not needed for this --use-api deployment.

If .env.publisher does not exist, create it:

Copy-Item .env.publisher.example .env.publisher

Open .env.publisher in your editor. Set these two lines for now:

SUPABASE_URL=https://hrwjogpklvgdvljrhptw.supabase.co
PUBLISHER_SECRET=PASTE_THE_SAME_GENERATED_SECRET

Leave the other template lines for step 6. Save it.
Run:

npm run publisher:run

Expect HTTP 200 and "website_only".
Deployment may say requested, waiting, or current.

Now open your test draft in Studio and click Publish product.
Keep its Pinterest checkbox OFF.
Run npm run publisher:run again, wait for the Cloudflare build to finish,
then run it again to verify the deployed version.
Open the product's permanent URL from Studio and check the store links.
The website will still need these manual runs until you finish step 5.

Keep .env.functions and .env.publisher private.
Do not send them to your friend or upload them to GitHub.
Supabase supplies the function's own privileged database key automatically.


5. MAKE THE PUBLISHER RUN BY ITSELF - OWNER ONLY
------------------------------------------------
a) In Supabase, open Database -> Extensions.
b) Enable pg_cron and pg_net. Confirm Vault is available in the dashboard.
c) Open SQL Editor -> New query.
d) Copy SQL B from the bottom of THIS file.
e) In the SQL Editor, replace YOUR-RANDOM-PUBLISHER-SECRET with the SAME secret
   you put in .env.functions and .env.publisher.
f) Run the SQL once.
g) Confirm "curated-publisher" appears under Cron and is active.

Do not save the filled SQL in Git or in this shared instruction file.
If it says Vault entries already exist, stop rerunning it; inspect the existing
Vault entries and Cron job instead. It may already be configured.

The job checks every 5 minutes, even with both PCs turned off.
Website build requests are spaced at least 15 minutes apart and capped at
12 per UTC day in this project. A publish is queued, not instantly live.

Check it once:
- Change the description of your published test product in Studio.
- Click Save published changes.
- Leave the terminals alone and watch Cloudflare for a deployment.
- Open the public page after the build.
- If it stalls, inspect catalog_state.last_error in Supabase Table Editor.


6. CONNECT YOUR PINTEREST ACCOUNT - OWNER ONLY
-----------------------------------------------
a) Open https://developers.pinterest.com/ and sign in to the Pinterest business
   account you want the Pins posted from.
b) Open My apps, create/register your app, and request Trial access if required.
c) Use these exact URLs:

Website:
https://curatedpick1.pages.dev

Privacy policy:
https://curatedpick1.pages.dev/privacy/

OAuth redirect URI:
https://curatedpick1.pages.dev/pinterest-connect/

Keep the trailing slash on the redirect URI.
Make sure these pages load publicly before submitting.
Describe the tool honestly: your team adds affiliate product details, and the
authorized account publishes Pins linking to your product pages.

d) Copy your Pinterest App ID and App Secret.
e) In Supabase -> Settings -> API Keys -> Legacy API keys, copy service_role.
   This PRIVATE key is ONLY for your owner connection script below.
   Do not put it in Studio, Cloudflare website variables, or your friend's copy.

Edit your private .env.publisher to contain:

SUPABASE_URL=https://hrwjogpklvgdvljrhptw.supabase.co
SUPABASE_SERVICE_ROLE_KEY=PASTE_YOUR_LEGACY_SERVICE_ROLE_KEY
PINTEREST_APP_ID=PASTE_YOUR_PINTEREST_APP_ID
PINTEREST_APP_SECRET=PASTE_YOUR_PINTEREST_APP_SECRET
PINTEREST_REDIRECT_URI=https://curatedpick1.pages.dev/pinterest-connect/
PINTEREST_API_ENV=sandbox
PUBLISHER_SECRET=PASTE_THE_SAME_GENERATED_SECRET

Save it. In your setup PowerShell terminal, run:

npm run pinterest:connect

f) Open the authorization URL printed in the terminal.
g) Log into YOUR intended Pinterest account and approve the app.
h) On the callback page, click "Copy connection response".
i) Paste it into the waiting terminal and press Enter.
   Keep the terminal open during this process.
j) The script saves tokens privately and prints available boards with IDs.
   Copy the numeric ID of the board you want.

If no usable board is listed, make sure your account has a board available in
the selected environment, then reconnect to list boards. Do not guess an ID.

Now edit .env.functions:
- Replace PINTEREST_APP_ID and PINTEREST_APP_SECRET with the same app values.
- Keep PINTEREST_API_ENV=sandbox.
- Keep PINTEREST_STANDARD_ACCESS=false.
- Change PINTEREST_PUBLISHING_ENABLED=true.

Save it and run:

npx supabase secrets set --env-file .env.functions

In Studio:
- Open one complete test product.
- Select Image Pin, or Video Pin with an actual video and JPG/PNG cover.
- Enter the numeric board ID from the terminal.
- Check "Post to Pinterest".
- Click Publish product / Save published changes.

The publisher first updates and verifies the website, then sends the Pin.
Use Refresh in Studio; detailed status is under More actions & publishing status. A video can need several scheduled runs.
Check pinterest_jobs.last_error in Supabase if the job fails.

Trial/sandbox Pins are testing content, visible only to their creator.
They are NOT yet your public marketing posts.


7. ENABLE REAL PUBLIC PINS AFTER PINTEREST APPROVES YOU
-------------------------------------------------------
a) In Pinterest's developer portal, request Standard access.
b) Submit the requested app details and a screen recording showing:
   - The OAuth login/approval flow.
   - Your working product form and Pinterest API integration.
c) Wait for Pinterest's approval. Setting a flag here cannot grant approval.

Official access rules:
https://developer.pinterest.com/docs/key-concepts/access-tiers/

While waiting, you can use your website and post Pins manually, using the
product page URL from Studio.

ONLY after Standard access is approved:

d) In .env.functions, set PINTEREST_PUBLISHING_ENABLED=false. Save and run:

npx supabase secrets set --env-file .env.functions

Wait at least two minutes and confirm no publisher run is still active before
switching credentials. You can check the function logs / Cron runs.

e) In .env.publisher, change PINTEREST_API_ENV=production. Save and run:

npm run pinterest:connect

Approve again and copy the PRODUCTION board ID printed by the script.

f) In .env.functions, set:

PINTEREST_API_ENV=production
PINTEREST_STANDARD_ACCESS=true
PINTEREST_PUBLISHING_ENABLED=false

Save and upload the settings:

npx supabase secrets set --env-file .env.functions

g) In Studio, update queued products to use the production board ID.
   For the simplest first public test, create a NEW product, use that board ID,
   check Post to Pinterest, then publish it. Existing sandbox test jobs are not
   automatically reposted as public Pins.

h) Set PINTEREST_PUBLISHING_ENABLED=true in .env.functions. Save and run:

npx supabase secrets set --env-file .env.functions

i) Wait for the publisher, then check the REAL public Pin:
   correct account -> correct image/video -> correct product page -> store links.

If you want to reuse a sandbox test product instead of a new product, update its
board ID first. Then in SQL Editor run the following, replacing PRODUCT_UUID
with that product's id. This resets only a job already marked sandbox:

update public.pinterest_jobs
set state='pending', pin_id=null, api_env=null, media_id=null, media_path=null,
    media_started_at=null, attempts=0, next_attempt_at=now(), last_error=null
where product_id='PRODUCT_UUID' and api_env='sandbox';

Do not reset an uncertain production job; first check whether its Pin exists.


8. GIVE YOUR LINUX FRIEND THE FORM
-----------------------------------
You do publisher/Pinterest setup once. Your friend does NOT repeat steps 4-7.

a) Create their own editor account and catalog_editors row using step 2.
b) Give them clean source from the GitHub repository:
   https://github.com/curatedpick1/curatedpick1
   Grant repository access if needed, or send a ZIP of that repository's source.
   Do not ZIP your working folder with its private .env files.
c) They install Node.js 24 and extract the source.
d) They open a terminal inside the extracted folder containing package.json.
   This repository has the app at its ROOT, with no nested Final folder.
e) They run:

npm ci
npm run studio

f) For daily use, they open Start Studio.sh. If their file manager opens it as
   text, enable Properties -> Permissions -> Allow executing as a program, then
   choose Run / Run in Terminal. Linux file managers vary; once configured,
   they do not need to type commands each time.
g) The launcher opens http://127.0.0.1:4333 and runs the server in the background.
   The form opens without a password for local drafts.
h) For publishing, they enter the public connection settings and THEIR editor
   email once, click Save settings, then unlock with THEIR password.

You both see shared Supabase products after signing in and refreshing.
Local drafts stay on the PC/browser where they were saved.
Both editors queue Pins for the ONE Pinterest account you connected.


YOUR DAILY ROUTINE AFTER SETUP
--------------------------------
1) Double-click Start Studio.cmd (Windows) or run Start Studio.sh (Linux).
2) Write the product once. Choose photos/video. Add matching affiliate links.
3) Use Save on this PC for unfinished/offline work.
4) When online and ready, unlock with your password, choose the board, and publish.
   Post to Pinterest starts checked. Your last entered board ID is remembered.
5) Supabase -> automatic Cloudflare rebuild -> verified product page -> Pin.
6) Click Refresh to check progress. Reconnecting alone does not publish drafts.

Changing the website poster later does not replace an existing Pin.
Prices are omitted; this app does not fetch real-time merchant prices.
No new paid automation subscription is required by this code, but provider free
limits still apply, and Pinterest approval is required for public API posting.


SQL A - ENABLE LOCAL EDITORS (STEP 2)
--------------------------------------
Copy only the SQL below, excluding the BEGIN/END marker lines.

BEGIN SQL A
begin;
-- Run after migrations 001 and 002. Only the project owner manages this allowlist.
create table if not exists public.catalog_editors (
  user_id uuid primary key references auth.users(id) on delete cascade,
  label text not null default '',
  created_at timestamptz not null default now()
);
alter table public.catalog_editors enable row level security;
revoke all on public.catalog_editors from public, anon, authenticated;
grant all on public.catalog_editors to service_role;

create or replace function public.is_catalog_editor()
returns boolean language sql stable security definer set search_path = '' as $$
  select exists(select 1 from public.catalog_editors where user_id = (select auth.uid()));
$$;
revoke all on function public.is_catalog_editor() from public;
grant execute on function public.is_catalog_editor() to anon, authenticated;

grant select on public.products to authenticated;
grant insert (id,title,description,category,tags,poster_url,poster_alt,stores,featured,published,publish_to_pinterest,pinterest_board_id,pin_media_type,pin_image_url,video_path),
      update (title,description,category,tags,poster_url,poster_alt,stores,featured,published,publish_to_pinterest,pinterest_board_id,pin_media_type,pin_image_url,video_path)
on public.products to authenticated;
drop policy if exists editor_read_products on public.products;
drop policy if exists editor_insert_products on public.products;
drop policy if exists editor_update_products on public.products;
create policy editor_read_products on public.products for select to authenticated using ((select public.is_catalog_editor()));
create policy editor_insert_products on public.products for insert to authenticated with check ((select public.is_catalog_editor()));
create policy editor_update_products on public.products for update to authenticated using ((select public.is_catalog_editor())) with check ((select public.is_catalog_editor()));

-- Status is read-only and deliberately excludes Pinterest tokens and publisher secrets.
create or replace function public.get_editor_status()
returns jsonb language plpgsql stable security definer set search_path = '' as $$
begin
  if not public.is_catalog_editor() then raise exception 'Editor access required' using errcode='42501'; end if;
  return jsonb_build_object(
    'catalog', (select jsonb_build_object('revision',revision,'deployed_revision',deployed_revision,'requested_at',requested_at,'last_error',last_error) from public.catalog_state where id=1),
    'pinterest_connected', exists(select 1 from public.pinterest_tokens),
    'jobs', coalesce((select jsonb_agg(jsonb_build_object('product_id',product_id,'state',state,'pin_id',pin_id,'api_env',api_env,'last_error',last_error)) from public.pinterest_jobs),'[]'::jsonb)
  );
end;
$$;
revoke all on function public.get_editor_status() from public, anon;
grant execute on function public.get_editor_status() to authenticated;

-- Replace the earlier blanket restriction, then grant only append/read media access.
drop policy if exists curated_media_private_management on storage.objects;
create policy curated_media_private_management on storage.objects as restrictive for all to anon, authenticated
using (bucket_id not in ('product-images','product-videos') or (select public.is_catalog_editor()))
with check (bucket_id not in ('product-images','product-videos') or (select public.is_catalog_editor()));
drop policy if exists editor_upload_media on storage.objects;
drop policy if exists editor_read_media on storage.objects;
create policy editor_upload_media on storage.objects for insert to authenticated
with check (bucket_id in ('product-images','product-videos') and (select public.is_catalog_editor()) and name like (select auth.uid())::text || '/%');
create policy editor_read_media on storage.objects for select to authenticated
using (bucket_id in ('product-images','product-videos') and (select public.is_catalog_editor()));
commit;
END SQL A


SQL B - SCHEDULE THE PUBLISHER (STEP 5)
---------------------------------------
Replace the secret placeholder ONLY in Supabase SQL Editor.
The Supabase project URL is already filled for your current project.
Copy only the SQL below, excluding the BEGIN/END marker lines.

BEGIN SQL B
-- Run once in Supabase SQL Editor AFTER replacing BOTH placeholders below.
-- Enable the Cron and pg_net extensions in Database > Extensions first.
-- DO NOT COMMIT A FILLED-IN COPY. These values belong in Supabase Vault.
do $$ begin
  if exists(select 1 from vault.secrets where name in ('curated_publisher_url', 'curated_publisher_secret')) then
    raise exception 'Publisher Vault entries already exist. Update them in Vault instead of creating duplicates.';
  end if;
end $$;
select vault.create_secret('https://hrwjogpklvgdvljrhptw.supabase.co/functions/v1/publisher', 'curated_publisher_url');
select vault.create_secret('YOUR-RANDOM-PUBLISHER-SECRET', 'curated_publisher_secret');

select cron.schedule('curated-publisher', '*/5 * * * *', $$
  select net.http_post(
    url := (select decrypted_secret from vault.decrypted_secrets where name = 'curated_publisher_url'),
    headers := jsonb_build_object(
      'Content-Type', 'application/json',
      'x-publisher-secret', (select decrypted_secret from vault.decrypted_secrets where name = 'curated_publisher_secret')
    ),
    body := '{}'::jsonb,
    timeout_milliseconds := 120000
  );
$$);

-- To stop all scheduled processing:
-- select cron.unschedule('curated-publisher');
END SQL B


REFERENCE FOR THE OWNER
-----------------------
Commands and settings above match the scripts included with this project.
Supabase function deployment:
https://supabase.com/docs/guides/functions/deploy
Pinterest Trial and Standard access:
https://developer.pinterest.com/docs/key-concepts/access-tiers/
